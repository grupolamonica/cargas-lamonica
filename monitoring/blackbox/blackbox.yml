# Módulos do blackbox-exporter usados pelos jobs blackbox-* do Prometheus.
modules:
  # Probe HTTP genérico — sucesso = qualquer 2xx.
  http_2xx:
    prober: http
    timeout: 10s
    http:
      preferred_ip_protocol: ip4
      follow_redirects: true
