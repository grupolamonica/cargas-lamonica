# Gerador Mock AngelLira — microservico HTTP (PDF do dossie a partir de JSON).
# Fluxo 100% local: recebe JSON -> renderiza PDF (layout AngelLira).
# SEM Selenium, SEM auth, SEM conexao com o portal AngelLira.
#
# Versões FIXADAS no conjunto conhecido-bom (venv de origem, python 3.13) para
# boot reproduzível entre rebuilds do deploy (evita um major novo quebrar o boot
# num deploy futuro que não tocou neste sidecar). Transitivas pinadas junto.
fastapi==0.141.1
starlette==1.3.1
pydantic==2.13.4
uvicorn==0.52.0
python-dotenv==1.2.2
pypdf==6.14.2
reportlab==5.0.0
svglib==2.0.2
lxml==6.1.1
pillow==12.3.0
