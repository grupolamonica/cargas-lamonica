fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
# GPT-4o Vision OCR client (Fase 1 da migração OCR — 2026-05-21)
openai>=1.50.0
# PDF page-split (Bug #2 da Fase F — CRLV com 2 páginas no Infosimples fallback)
pypdf>=4.0.0
# PDF→JPEG rasterização para GPT-4o Vision (RNTRC, selfie-cnh recebidos como PDF)
pymupdf>=1.24.0
# Normalização de imagem p/ OCR (DC-306): HEIC/HEIF (iPhone) e outros formatos
# → JPEG antes da Infosimples/Vision. pillow-heif registra o opener HEIC no Pillow.
Pillow>=10.0.0
pillow-heif>=0.16.0
