INFOSIMPLES_TOKEN=COLE_SEU_TOKEN_AQUI

# ── OCR Providers ─────────────────────────────────────────────────────────────
# Controla qual engine processa cada tipo de documento:
#   "infosimples" — API paga (precisão alta, treinada em docs BR)
#   "local"       — EasyOCR rodando na propria maquina (gratis, offline)
#
# CNH e CRLV permanecem sempre no Infosimples (parsers deles agregam valor).
# Para usar "local", rode uma vez: pip install -r requirements-ocr.txt

OCR_COMPROVANTE_PROVIDER=infosimples
OCR_CARTAO_CNPJ_PROVIDER=local
