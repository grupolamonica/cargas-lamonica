# Dependencias OPCIONAIS para OCR local (comprovante de residencia + cartao CNPJ).
# Instalar SOMENTE se OCR_*_PROVIDER=local no .env.
# Motivo: stack pesada (~600MB com torch). Deixar fora do requirements.txt padrao
# para nao inflar demo que usa apenas Infosimples.
#
# Engine: EasyOCR (Apache 2.0, suporta PT-BR, compativel com Python 3.13).
# Alternativa descartada: PaddleOCR — paddlepaddle sem build oficial para Python 3.13.
#
# pypdfium2: render de PDF -> imagem (pure-Python wrapper de pdfium, sem C++).
#            Necessario porque EasyOCR trabalha com imagens; PDFs precisam ser
#            rasterizados antes. Compativel Python 3.13, ~15MB.

easyocr>=1.7.2
pypdfium2>=4.30.0
# easyocr puxa: torch, torchvision, numpy, Pillow, opencv-python-headless, scipy
